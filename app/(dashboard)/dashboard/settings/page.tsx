import { DashboardShell } from "@/components/shell";

export const metadata = {
  title: "Settings",
};

export default async function SettingsdPage() {
  return (
    <div>
      <h1>Settings page...</h1>
    </div>
  );
}
